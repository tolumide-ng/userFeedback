import { Button } from "../../atoms/Button";

export const Header = () => {
    const headerTitle = "Feedback Gorm";

    return (
        <header>
            <h1>{headerTitle}</h1>
            {/* <Button text="" /> */}
        </header>
    );
};
